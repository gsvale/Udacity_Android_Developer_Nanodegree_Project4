<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/recipe_ll"
    android:layout_width="@dimen/recipe_widget_width"
    android:layout_height="@dimen/recipe_widget_height"
    android:layout_margin="@dimen/recipe_widget_margin"
    android:layout_gravity="center"
    android:gravity="center"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/recipe_image_iv"
        android:layout_width="@dimen/recipe_widget_image_size"
        android:layout_height="@dimen/recipe_widget_image_size"
        android:layout_gravity="center"
        android:contentDescription="@string/recipe_image"
        android:src="@drawable/ic_recipe_icon" />

    <TextView
        android:id="@+id/recipe_name_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="Recipe Name"
        android:gravity="center"
        android:layout_gravity="center"
        android:maxLines="2"
        android:ellipsize="end"
        android:layout_marginTop="@dimen/recipe_widget_margin"
        android:textColor="@color/colorWhite"
        android:textStyle="normal"
        android:textSize="@dimen/general_text_size_13"
        />

</LinearLayout>